# 3D README Preview (assets and embed instructions)

I've added an animated SVG preview `assets/orbiting-skills.svg` to the repository — a lightweight "skill orbit" animation you can embed in your README as a cinematic hero preview.

How to embed into your README.md

```markdown
<p align="center">
  <a href="https://albin170.github.io/">
    <img src="/assets/orbiting-skills.svg" alt="3D skill orbit preview" />
  </a>
</p>
```

Notes and next steps

- The SVG uses SMIL animations which render when the image is embedded as an <img> or via raw SVG (GitHub supports this). If you prefer a WebP hero animation, I can help generate an optimized animated WebP from Blender frames.
- If you want, I can update your main `README.md` to include the preview inline (I didn't change it automatically to avoid accidental content overwrite). Reply "update README" and I'll insert the preview into your README at the top.

If you want the README updated now, tell me where you'd like it inserted (top, below existing hero, or at the end).